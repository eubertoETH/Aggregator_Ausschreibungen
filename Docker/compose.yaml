services:
  app:
    build:
      context: ..
      dockerfile: Docker/Dockerfile
    image: ghcr.io/${GITHUB_REPOSITORY:-local/service-template}:local
    env_file:
      - ../.env
    ports:
      - "${SERVICE_PORT:-8080}:8080"
    restart: unless-stopped
